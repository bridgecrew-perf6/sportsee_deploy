import React from 'react';
import Build from '../components/Build';

/**
 * Page Community with component Build - page in building
 * @returns {JSX} - React component - Page
 */
const Community = () => {
    return (
        <Build />
    );
};

export default Community;