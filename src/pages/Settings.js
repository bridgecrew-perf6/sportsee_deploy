import React from 'react';
import Build from "../components/Build"


/**
 * Page Settings with component Build - page in building
 * @returns {JSX} - React component - Page
 */
const Settings = () => {
    return (
        <Build />
    );
};

export default Settings;